{
  "name": "server",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "postinstall": "yarn build",
    "start": "node .build/index.js",
    "watch-nodemon": "nodemon --watch .build .build/index.js",
    "watch": "concurrently \"yarn build --watch\" \"yarn watch-nodemon\""
  },
  "dependencies": {
    "concurrently": "^6.2.0",
    "electron-store": "^8.0.0",
    "fastify": "^3.16.2",
    "fastify-cors": "^6.0.1",
    "rxjs": "^7.1.0",
    "serialport": "^9.1.0",
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "@types/serialport": "^8.0.1",
    "@types/xml2js": "^0.4.8",
    "nodemon": "^2.0.7",
    "typescript": "^4.3.2"
  }
}
